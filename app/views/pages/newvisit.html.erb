
      <%= form_for(@visit) do |f| %>
        <% if @visit.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

            <ul>
            <% @visit.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <h2 class="newvisith2">What are you here for today?</h2>
        <br/>
        <br/>
        <br/>

        <div class="row">
          <!-- <div class="col span-1-of-3">
              <label>Reason for visitng</label>
          </div> -->
            <div class="col span-1-of-2">
            <%= select_tag "course", options_from_collection_for_select(@courses, "id", "name") %>
            </div>
        </div>
<br/>
<br/>
<br/>
<br/>
        <!-- <div class="row">
            <div class="col span-1-of-2">
                <label>&nbsp;</label>
            </div>
            <div class="col span-2-of-2">
          <%= f.submit "Check In", class: "btn btn-1" %>
            </div>
        </div> -->

      <% end %>
